[pytest]
testpaths = tests
# Async tests are marked with @pytest.mark.anyio; pin the backend to asyncio so
# each async test runs once rather than once per installed backend.
markers =
    anyio: run the test on the anyio asyncio backend
filterwarnings =
    ignore::DeprecationWarning:fastapi.*
